'''
This Class is only used for Development.
'''

# import dbConnection  # Connects when import is called
import json
from config import *

#testDB = dbConnection.dbConnection();
#print(testDB.run_sql("SHOW DATABASES"))

# jsonConfig = config()
# jsonConfig.read_config_file()
# print(jsonConfig.config.get("database").get("user"))
